[postgresql]
dbname = deux
user = consumer_adm
password = criandta0
host = db_postgres
port = 5432

[logging]
log_file = webhook_receiver.log
log_level = INFO

[data]
regex_pattern = key=\w+,\s*symbol=\w+-\w+,\s*side=(buy|sell),\s*indicator=\d+
data_fields = key,symbol,side,indicator

[table]
table_name = webhook_data

[rabbitmq]
host = localhost  # IP ou hostname do RabbitMQ
queue_name = webhook_queue
